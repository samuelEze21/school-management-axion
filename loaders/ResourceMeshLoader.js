/**
 * ResourceMeshLoader - loads resource nodes (e.g. for mesh/routing). Stub returns empty object.
 */
module.exports = class ResourceMeshLoader {
    constructor(opts = {}) {
        this.opts = opts;
    }

    load() {
        return {};
    }
};
